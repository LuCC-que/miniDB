{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "test - Debug",
      "program": "${workspaceFolder}/build/tests",
      "args": [],
      "cwd": "${workspaceFolder}",
    },
    {
      "type": "lldb",
      "request": "launch",
      "name": "main test",
      "program": "${workspaceFolder}/build/miniDB",
      "args": [
        "new1.db"
      ],
      "cwd": "${workspaceFolder}",
    }
  ]
}